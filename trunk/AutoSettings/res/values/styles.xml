<?xml version="1.0" encoding="utf-8"?>

<resources>

<style name="NoBackground" parent="android:Theme.Dialog">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    <!-- item name="android:windowBackground">@null</item -->
    <item name="android:windowBackground">@drawable/transparent_background</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:colorForeground">#fff</item>
</style>

<style name="SectionHeader">
    <item name="android:textAppearance">@android:style/TextAppearance.Large.Inverse</item>
    <item name="android:gravity">center_horizontal</item>
    <item name="android:layout_margin">1sp</item>
    <item name="android:background">@drawable/action_bg_gradient</item>

</style>

<style name="ActionButton" parent="android:Widget.Button">
    <item name="android:paddingTop">10sp</item>
    <!-- don't set item name="android:focusableInTouchMode" to true, it makes
         the button take focus on touch instead of invoking it. -->
</style>

</resources>
